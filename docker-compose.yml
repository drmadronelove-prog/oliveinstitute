services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: olive_institute
    ports:
      - "5432:5432"
    volumes:
      - olive_institute_postgres:/var/lib/postgresql/data

volumes:
  olive_institute_postgres:
