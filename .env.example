DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sati_lms?schema=public"

# NextAuth
NEXTAUTH_SECRET="replace-with-a-long-random-string"
NEXTAUTH_URL="http://localhost:3000"

# Email (dev mode logs instead of sending; set RESEND_API_KEY to send for real)
RESEND_API_KEY=""
EMAIL_FROM="Sati Center LMS <noreply@saticenter.org>"

# File storage (local disk for now)
UPLOADS_DIR="./uploads"
